import React from "react";

interface Props {
  imgUrl: string;
  alt: string;
  value: string;
  title: string;
  textStyles: string;
  isAuthor: string;
}

const Metric = ({ imgUrl, alt, value, title, textStyles, isAuthor }: Props) => {
  return <div>Metric</div>;
};

export default Metric;
